export function getThemeStyles() {
    return `
      /* Theme styles - color schemes and theme variables */
      /* Currently using inline color values throughout */
      /* Future: Extract color variables here for theming support */
    `;
}
